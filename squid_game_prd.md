# 제품 요구사항 정의서 (PRD) - 초등학생용 오징어 게임 앱

## 1. 프로젝트 개요
* **프로젝트명:** 교실에서 즐기는 초등학생 오징어 게임
* **목적:** 교실 내 전자칠판을 활용하여 학생들이 팀을 나누어 협동하고 경쟁할 수 있는 간단하고 몰입감 있는 미니 게임 세트 제공.
* **대상 사용자:** 초등학생 (터치 기반의 직관적이고 쉬운 조작 필요)
* **주요 환경:** 교실 내 전자칠판 (가로 해상도 중심, 터치스크린)

## 2. 목표 및 기대 효과
* **학생 참여 유도:** 인기 콘텐츠인 '오징어 게임'의 컨셉을 차용하여 학생들의 흥미와 수업 집중도를 높임.
* **협동심 강화:** A팀, B팀으로 나눈 팀전 시스템을 통해 소속감과 응원 문화 형성.
* **즉각적인 플레이:** 별도의 설치나 서버 구축 없이 브라우저에서 바로 실행 가능한 단일 웹 환경(HTML/CSS/JS) 제공.

## 3. 사용자 경험 흐름 (User Flow)
1. **인트로 화면:** 게임 타이틀 확인 및 [참여하기] 버튼 터치.
2. **팀 선택 화면:** 현재 도전할 학생의 소속 팀(A팀/B팀) 선택.
3. **라운드 1 (무궁화 꽃이 피었습니다):** 술래(영희)의 음성을 듣고 타이밍에 맞춰 버튼을 연타하여 전진. 술래가 볼 때 움직이면 탈락.
4. **라운드 2 (달고나 떼기):** 40초의 제한 시간 내에 랜덤으로 주어진 도형의 점선을 따라 전자칠판에 손을 대고 그림. 선을 벗어나거나 시간이 초과되면 탈락.
5. **라운드 3 (줄다리기):** 4개의 줄 중 하나를 선택. 1개의 썩은 줄(랜덤)을 선택하면 탈락, 튼튼한 줄을 선택하면 최종 생존.
6. **결과 화면:** * **생존 시:** 축하 화면과 함께 선택했던 팀의 점수 1점 증가.
   * **탈락 시:** 탈락 사유 명시 및 실패 화면 노출.
   * 공통적으로 [다음 도전자] 버튼을 눌러 팀 선택 화면으로 회귀.

## 4. 핵심 기능 요구사항

### 4.1. 공통 기능
* **상태 관리:** A팀과 B팀의 누적 점수를 전역으로 관리하고 상단에 실시간 표시.
* **화면 전환:** SPA(Single Page Application) 방식으로 각 라운드(DOM 요소)의 `display` 속성을 토글하여 화면 전환 구현.
* **반응형/터치 최적화:** 모바일 및 전자칠판에서의 터치 이벤트(`touchstart`, `touchmove` 등) 지원 및 텍스트 드래그 방지(`user-select: none`).

### 4.2. 라운드 1: 무궁화 꽃이 피었습니다
* **Web Speech API:** 브라우저 내장 TTS를 사용하여 "무궁화 꽃이 피었습니다" 음성 출력. 음성 재생 속도를 랜덤(0.8x ~ 1.6x)으로 설정하여 긴장감 부여.
* **상태 판정:** 음성이 끝나는 즉시 술래가 뒤를 돌아보는 상태(`isLooking = true`)로 전환되며 1.5초~3초 대기. 이 상태에서 [달리기] 버튼 터치 시 탈락 처리.
* **진척도:** 버튼 1회 터치 시 진행률 바 15% 증가. 100% 도달 시 라운드 2로 이동.

### 4.3. 라운드 2: 달고나 떼기
* **타이머:** 40초 카운트다운 구현. 0초 도달 시 강제 탈락.
* **랜덤 도형:** 동그라미, 세모, 네모, 하트 중 하나를 무작위로 Canvas에 점선으로 렌더링.
* **터치 드로잉 및 충돌 감지 (Canvas API):**
  * 사용자의 터치 궤적을 붉은색으로 그림.
  * `ctx.isPointInStroke()`를 활용하여 터치 좌표가 지정된 경로(Path)를 크게 벗어났는지 실시간 검사. 벗어날 경우 즉시 탈락.
* **성공 판정:** 도형의 주요 꼭짓점/경유지(Checkpoints)를 모두 지나간 경우 성공으로 판정하여 라운드 3으로 이동.

### 4.4. 라운드 3: 줄다리기
* **랜덤 지뢰 로직:** 4개의 시각적 줄(`div`) 중 1개를 랜덤으로 '끊어지는 줄'로 설정.
* **결과 판정:** * 안전한 줄 선택 시: 초록색 하이라이트 및 우승 화면으로 이동. 팀 점수 +1.
  * 끊어지는 줄 선택 시: 끊어지는 시각적 애니메이션 제공 및 탈락 화면으로 이동.

## 5. 시스템 및 기술 아키텍처
* **UI/UX:** HTML5, CSS3 (Keyframe 애니메이션, Flexbox 레이아웃, Jua 웹 폰트 적용)
* **로직 및 상태 제어:** Vanilla JavaScript (ES6)
* **그래픽 렌더링:** HTML5 `<canvas>` API (라운드 2 달고나 구현)
* **음성 합성:** `window.speechSynthesis` API (라운드 1 영희 목소리)
* **배포 방식:** 모든 리소스가 포함된 단일 `index.html` 파일 제공 (오프라인/로컬 브라우저 환경에서 즉시 실행 가능)

## 6. 향후 개선 가능 사항 (v2.0)
* **효과음/배경음 추가:** 게임 몰입도를 위한 긴장감 있는 BGM 및 탈락/성공 효과음 추가.
* **난이도 조절 기능:** 선생님이 달고나 제한 시간, 무궁화 꽃 이동 속도 등을 설정할 수 있는 관리자 패널 추가.
* **모바일 연동 (멀티플레이):** Firebase 등을 도입하여 학생들의 스마트폰을 컨트롤러로 사용하여 다수결로 줄을 당기는 진정한 형태의 멀티플레이 줄다리기 구현.
